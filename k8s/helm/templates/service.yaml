apiVersion: v1
kind: Service
metadata:
  name: {{ include "resumeopt.fullname" . }}
  labels:
    {{- include "resumeopt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resumeopt.selectorLabels" . | nindent 4 }}
